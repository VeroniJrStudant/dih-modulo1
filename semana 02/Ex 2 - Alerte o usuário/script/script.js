function confirmaAcao () {
     var confirma = window.confirm('Verifique antes de confirma');
        if (comfirma == true)
        {
            window.alert("Após o confirme não tem como voltar.")
        }
        else 
        {
            window.alert("Ok, deixamos para a próxima!")
        }
}